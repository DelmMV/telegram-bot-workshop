# 🚀 Миграция на Vercel для Telegram WebApp

## 🎯 Проблема с GitHub Pages
GitHub Pages **не поддерживает** кастомные заголовки (`_headers` файл игнорируется), из-за чего Telegram не передает `initData` в iframe.

## ✅ Решение: Vercel
Vercel **полностью поддерживает** Telegram WebApp API и позволяет настроить правильные заголовки.

---

## 📋 Пошаговая инструкция (15 минут)

### Шаг 1: Регистрация на Vercel (2 минуты)

1. Перейдите на **vercel.com**
2. Нажмите **"Continue with GitHub"**
3. Авторизуйтесь через GitHub
4. В настройках разрешите доступ к репозиторию `telegram-bot-workshop`

### Шаг 2: Импорт проекта (3 минуты)

1. На Vercel нажмите **"Add New..." → "Project"**
2. Выберите репозиторий `telegram-bot-workshop`
3. В разделе **"Framework Preset"** выберите **"Next.js"**
4. В разделе **"Root Directory"** выберите **`/webapp`**
5. Нажмите **"Deploy"**

### Шаг 3: Настройка Environment Variables (2 минуты)

После деплоя перейдите в **Settings → Environment Variables**:

| Переменная | Значение |
|------------|----------|
| `NEXT_PUBLIC_API_BASE_URL` | `https://service.monopiter.ru` |

Нажмите **"Save"** и **"Redeploy"**.

### Шаг 4: Получение нового URL (1 минута)

После деплоя вы получите URL вида:
```
https://your-project-name.vercel.app
```

**Скопируйте этот URL** - он понадобится для BotFather.

### Шаг 5: Обновление BotFather (3 минуты)

1. Откройте Telegram → **@BotFather**
2. Выберите вашего бота → **"Bot Settings"**
3. **"Menu Button"** → **"Edit Web App"**
4. Вставьте новый URL: `https://your-project-name.vercel.app`
5. Сохраните

### Шаг 6: Тестирование (4 минуты)

1. Откройте вашего бота в Telegram
2. Нажмите кнопку **"Mini App"** (или меню)
3. Проверьте консоль (F12):
   - Должно быть: `[DEBUG] Telegram WebApp initialized`
   - Должно быть: `[DEBUG] initData length: 450+`
   - **НЕ должно быть**: `[DEBUG] Telegram.WebApp not found`

---

## 📁 Измененные файлы

### ✅ `webapp/vercel.json` (создан)
```json
{
  "outputDirectory": "out",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "ALLOWALL" },
        { "key": "Access-Control-Allow-Origin", "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET, POST, OPTIONS" },
        { "key": "Access-Control-Allow-Headers", "value": "Content-Type, X-Telegram-Init-Data" },
        { "key": "Content-Security-Policy", "value": "frame-ancestors 'self' https://telegram.org https://*.telegram.org" }
      ]
    }
  ]
}
```

### ✅ `webapp/next.config.js` (упрощен)
```javascript
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: { unoptimized: true },
}
module.exports = nextConfig
```

### ✅ `webapp/.env.local` (уже настроен)
```bash
NEXT_PUBLIC_API_BASE_URL=https://service.monopiter.ru
```

---

## 🎯 Что изменится

| Аспект | GitHub Pages | Vercel |
|--------|--------------|--------|
| **Поддержка WebApp API** | ❌ Нет | ✅ Да |
| **Кастомные заголовки** | ❌ Нет | ✅ Да |
| **initData** | ❌ Пустой | ✅ Передается |
| **URL** | `delmmv.github.io/...` | `your-project.vercel.app` |
| **Стоимость** | Бесплатно | Бесплатно (хобби-план) |

---

## 🚨 Важные замечания

1. **После миграции обязательно обновите URL в BotFather**
2. **Старый GitHub Pages можно оставить как резервную копию**
3. **Vercel автоматически деплоит при каждом push в GitHub**
4. **Если нужно вернуться - просто измените URL в BotFather обратно**

---

## 🔍 Проверка после миграции

### Успех ✅:
```
[DEBUG] Telegram WebApp initialized
[DEBUG] initData length: 450
[API] 🚀 Request: { hasInitData: true }
[API] ✅ Success: { data: { ok: true } }
```

### Ошибка ❌:
```
[DEBUG] Telegram.WebApp not found
❌ Ошибка авторизации
```

---

## 🆘 Если что-то пошло не так

1. **Проверьте URL в BotFather** - должен быть верным
2. **Проверьте Environment Variables** в Vercel
3. **Посмотрите логи Vercel** → раздел "Logs"
4. **Перезапустите приложение** в Telegram (закройте и откройте снова)

---

## 📞 Поддержка

Если возникнут проблемы:
- **Vercel Logs**: Dashboard → Your Project → "Logs"
- **Telegram Console**: F12 в браузере
- **API Server**: Проверьте логи на `service.monopiter.ru`

---

## ✅ Готово!

После миграции на Vercel проблема с `initData` будет **полностью решена**. Telegram корректно передаст данные, и ваше приложение заработает как надо.