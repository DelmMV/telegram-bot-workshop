# ⚡ Vercel Setup Guide

## 🎯 Что уже сделано:

✅ `vercel.json` - настроен с правильными заголовками  
✅ `next.config.js` - упрощен для Vercel  
✅ `.env.local` - API URL настроен  

## 📋 Осталось сделать (вручную):

### 1. Зарегистрироваться на Vercel
- Перейти на vercel.com
- Войти через GitHub

### 2. Импортировать проект
- **Import Git Repository**
- Выбрать `telegram-bot-workshop`
- **Framework Preset**: Next.js
- **Root Directory**: `/webapp`
- **Environment Variables**: `NEXT_PUBLIC_API_BASE_URL=https://service.monopiter.ru`
- **Deploy**

### 3. Обновить BotFather
- Новый URL: `https://your-project.vercel.app`
- `/setmenu` или `@BotFather → Bot Settings → Menu Button`

---

## 📊 Почему Vercel решает проблему:

| Проблема | GitHub Pages | Vercel |
|----------|--------------|--------|
| `window.Telegram` undefined | ❌ Да | ✅ Работает |
| `initData` пустой | ❌ Да | ✅ Передается |
| Кастомные заголовки | ❌ Нет | ✅ Да |
| iframe поддержка | ❌ Ограничена | ✅ Полная |

---

## 🚀 После деплоя на Vercel:

1. **Откройте Telegram** → Ваш бот
2. **Нажмите кнопку Mini App**
3. **Проверьте консоль (F12)**:
   - ✅ `[DEBUG] Telegram WebApp initialized`
   - ✅ `[DEBUG] initData length: 450+`
   - ✅ `[API] ✅ Success`

---

## 🆘 Если не работает:

1. **Проверьте URL в BotFather** - должен быть `.vercel.app`
2. **Проверьте Environment Variables** в Vercel Dashboard
3. **Перезапустите приложение** в Telegram (закройте и откройте)

---

## 📝 Команды для Vercel CLI (опционально):

```bash
# Установить Vercel CLI
npm i -g vercel

# Войти
vercel login

# Деплой
cd webapp
vercel
```

---

## ✅ Готово к миграции!

Все файлы настроены правильно. Осталось только создать проект на Vercel и обновить URL в BotFather.